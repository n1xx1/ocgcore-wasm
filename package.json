{
  "name": "ocgcore-wasm",
  "version": "0.0.4",
  "type": "module",
  "author": {
    "name": "n1xx1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/n1xx1/ocgcore-wasm.git"
  },
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/*",
    "lib/*",
    "src"
  ],
  "browser": {
    "module": false
  },
  "license": "MIT",
  "scripts": {
    "clear:lib": "rm -rf dist/*.js dist/*.js.map",
    "build:emscripten": "chmod a+x ./build.sh && ./build.sh",
    "build:lib": "npm run clear:lib && esbuild ./src/index.ts --analyze --bundle --external:module --format=esm --target=es2020 --splitting --minify --sourcemap --outdir=dist",
    "build:test": "esbuild ./tests/test.ts --analyze --bundle --platform=node --format=esm --target=es2020 --splitting --minify --sourcemap --outdir=tests/dist",
    "build:types": "tsc --project tsconfig.types.json && api-extractor run",
    "build": "npm run build:emscripten && npm run build:lib && npm run build:types"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.36.4",
    "@types/emscripten": "^1.39.7",
    "@types/node": "^20.5.9",
    "esbuild": "^0.19.2",
    "esno": "^0.17.0",
    "typescript": "^5.2.2"
  }
}
